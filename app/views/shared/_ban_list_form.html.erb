<%= form_for(@ban_list) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_field :title, class: 'form-control' %>
  </div>
  <%= f.hidden_field :match_id, :value => @match.id %>
  <%= f.submit "追加", class: "btn btn-primary" %>
<% end %>